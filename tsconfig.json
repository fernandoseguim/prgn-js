{
  "compilerOptions": {
    "target": "ES6",                         // Define a versão do ECMAScript que o código JS compilado deve seguir
    "module": "commonjs",                    // Define o sistema de módulos
    "strict": true,                          // Ativa verificações rigorosas de tipo
    "esModuleInterop": true,                 // Suporte para interoperabilidade com módulos ES
    "outDir": "./dist",                      // Diretório de saída para os arquivos compilados
    "rootDir": "./src"                       // Diretório raiz dos arquivos TypeScript
  },
  "include": [
    "src/**/*"                               // Inclui todos os arquivos TypeScript dentro da pasta src
  ],
  "exclude": [
    "node_modules"                           // Exclui a pasta node_modules do processo de compilação
  ]
}
